- 统一使用ApiResponse<T>包装所有控制器返回结果，包含code、message和data字段。
- 业务异常统一抛出BusinessException，由GlobalExceptionHandler捕获并转换为标准错误响应。
- DTO命名遵循明确后缀规范：请求类使用Request后缀，响应类使用Response后缀。
- 服务层方法通过Authentication对象获取当前登录用户ID，确保业务逻辑与用户身份绑定。