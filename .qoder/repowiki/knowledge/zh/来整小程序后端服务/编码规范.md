- 所有API响应均使用`ApiResponse<T>`包装，成功时code为0，失败时携带具体错误码。
- Service层方法通过构造函数注入依赖，并使用`@Transactional`注解保证业务操作的事务一致性。
- 业务异常统一抛出`BusinessException`，由全局异常处理器捕获并转换为对应的HTTP 400响应。
- Controller层通过`Authentication`对象获取当前登录用户ID，实现业务逻辑与用户身份的解耦。