- **配置管理**：通过`application.yml`加载配置，支持从`local-secrets.yml`导入敏感信息（如微信AppSecret、JWT密钥）。
- **容器化部署**：提供`Dockerfile`和`docker-compose.yml`用于本地环境一键启动（包含PostgreSQL和Redis服务）。
- **数据库迁移**：应用启动时自动执行`src/main/resources/db/migration`下的SQL脚本初始化或更新表结构。