- 后端统一使用`ApiResponse<T>`封装接口响应，并通过`GlobalExceptionHandler`集中处理业务异常。
- 前后端均采用模块化目录结构，后端按业务领域（auth/activity/common）划分包，前端按页面（pages）和工具（utils）组织代码。
- 数据库变更通过Flyway迁移脚本（V1__init_core_tables.sql等）进行版本化管理，确保 schema 演进的可追溯性。