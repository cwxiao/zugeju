- **分层设计**：采用标准前后端分离，前端为微信原生小程序，后端为Spring Boot 3 + Java 21。
- **模块化后端**：后端按业务域划分为auth（鉴权）、activity（活动）、invite（邀请回执）、expense（费用）、settlement（结算）、stats（统计）等模块。
- **数据驱动**：以PostgreSQL为核心存储，通过Flyway管理版本，Redis处理会话与缓存，强调主实体与行为事件（如邀请流水）分离的设计原则。
- **文档结构**：包含从产品PRD、页面文案、数据库建模到接口定义、部署运维的全链路文档，并辅以专项改进设计说明。