- 金额统一以“分”为单位在后端存储和计算，前端展示时格式化为元，避免浮点误差。
- 邀请链路采用“主状态表(activity_invites) + 事件流水表(invite_events) + 聚合统计表(activity_rsvp_stats)”的组合模式，确保实时性与可追溯性。
- 后端接口统一返回 `{code, message, data}` 结构，成功码为0，失败码使用4位数字（如4001）。
- 数据库表名采用复数形式（如users, activities），主键统一使用UUID，并配备自动更新时间的触发器。