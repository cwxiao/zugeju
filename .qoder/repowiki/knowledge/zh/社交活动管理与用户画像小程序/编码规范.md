- 页面逻辑中普遍采用 `getApp().hasLoginState()` 进行前置登录校验，未登录时统一触发授权弹窗而非直接跳转。
- 网络请求统一通过 `utils/request.js` 发起，并在页面层捕获 `isAuthExpiredError` 以处理会话过期后的重登录流程。
- 用户头像与昵称等敏感信息在登录前通过本地缓存 (`wx.getStorageSync('profile')`) 预填充，提升静默登录成功率。