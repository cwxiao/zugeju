- 页面逻辑文件统一命名为 `index.js` 并存放于以功能命名的子目录下（如 `pages/home/index.js`）。
- API 请求统一通过 `utils/request.js` 封装，自动注入 `Bearer Token` 并处理 401/403 鉴权失效跳转。
- 用户认证状态在 `app.js` 全局维护，并通过 `wx.setStorageSync` 持久化 `token` 与 `user` 信息。