- 页面逻辑统一采用 Page({}) 构造器，并在 onShow 生命周期中执行登录态校验与数据加载。
- API 请求统一通过 utils/request.js 封装，自动注入 Bearer Token 并处理 401/403 鉴权失效后的自动登出逻辑。
- 全局状态管理依赖 getApp().globalData 存储用户信息与 Token，并通过 wx.setStorageSync 实现持久化同步。